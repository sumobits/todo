/**
 * @format
 */
import React from 'react';
import {
	StyleSheet,
	View,
} from 'react-native';
import {
	Button,
	FormInput,
} from './common';
import {
	Color,
	Fonts,
} from '../utils';

const TaskView = () => {
	return (
		<View>

		</View>
	);
};

const styles = StyleSheet.create({

});

export default TaskView;
